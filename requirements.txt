# ExplainShot runtime dependencies

# UI
PyQt6==6.9.1
qasync==0.27.1

# Global hotkeys (Windows-friendly, cross-platform)
pynput==1.8.1

# Screen capture + image processing
Pillow==11.3.0

# AI provider — OpenAI-compatible HTTP client
httpx==0.28.1

# Markdown rendering in chat transcript
markdown2==2.5.4
